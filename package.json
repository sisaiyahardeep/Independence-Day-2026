{
  "name": "static-independence-day",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --host=0.0.0.0 --port=3000",
    "build": "mkdir -p dist && cp index.html style.css script.js *.png *.webp *.mp3 dist/ 2>/dev/null || true"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
