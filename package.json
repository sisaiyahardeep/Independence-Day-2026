{
  "name": "static-independence-day",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --host=0.0.0.0 --port=3000",
    "build": "echo 'Static site, no build needed for GitHub Pages'"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
